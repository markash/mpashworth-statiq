@using FromZeroToHero.Models.ContentTypes
@model List<Page>
@foreach (var subPage in Model)
{
                    <a href="#" class="block px-3 py-2 rounded-md text-base font-medium text-gray-700 hover:text-gray-900 hover:bg-gray-50">@subPage.Title</a>
}